ul.sidebar
	each val, index in sections.sections
		li
			a(href="#section_" + index)= index
			ul.sub
				each e in val
					li
						a(href="#method_" + e.method + e.path.replace(/\/|}|{/g, ''))= e.path
				
