$(function() {

	// var $nav = $('.sidebar');

	$('.sidebar').scrollspy({
		// min: $nav.offset().top,
		// onEnter: function(element, position) {
		// 	$nav.addClass('fixed');
		// },
		// onLeave: function(element, position) {
		// 	$nav.removeClass('fixed');
		// }
	});

});